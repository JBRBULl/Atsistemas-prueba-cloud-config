# HTTP Server
server:  
  port: 0   # HTTP port
eureka:
  instance:
    hostname: localhost

# Discovery Server Access
eureka:  
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/
